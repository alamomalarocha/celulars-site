import { fileURLToPath } from 'node:url';import path from 'node:path';import { loadConfig } from './config.js';import { activationReport } from './activation.js';
if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url)){try{const report=activationReport(loadConfig());console.log(JSON.stringify(report,null,2));if(report.status!=='READY')process.exitCode=2;}catch(error){console.log(JSON.stringify({status:'NOT_READY',pending:['configuration'],error:error instanceof Error?error.message:'invalid configuration',warning:'Nenhuma ativação foi executada.'},null,2));process.exitCode=2;}}
