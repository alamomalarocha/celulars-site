import { existsSync,readFileSync } from 'node:fs';import path from 'node:path';const root=process.cwd();const required=['.env.staging.example','.env.production.example','apps/platform/deploy/ACTIVATION.md','apps/platform/deploy/INFRASTRUCTURE.md','scripts/platform-build-target.mjs'];const missing=required.filter(file=>!existsSync(path.join(root,file)));const packageJson=JSON.parse(readFileSync(path.join(root,'package.json'),'utf8'));const commands=['platform:build:staging','platform:build:production','platform:activation:check','platform:bootstrap-admin'];for(const command of commands)if(!packageJson.scripts?.[command])missing.push(`package script ${command}`);const report={status:missing.length?'NOT_READY':'READY_FOR_EXTERNAL_PROVISIONING',missing,publicDistUntouched:true,deploymentExecuted:false,warning:'Este comando não publica nem cria recursos externos.'};console.log(JSON.stringify(report,null,2));if(missing.length)process.exitCode=2;
